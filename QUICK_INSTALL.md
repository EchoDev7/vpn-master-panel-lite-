# 🚀 نصب سریع VPN Master Panel

## یک دستور، نصب کامل!

```bash
curl -sSL https://raw.githubusercontent.com/EchoDev7/vpn-master-panel/main/quick-install.sh | sudo bash
```

**همین!** 🎉

---

## ✅ پیش‌نیازها

- Ubuntu 22.04 (سرور تمیز)
- حداقل 2GB RAM
- دسترسی root

---

## 📺 چه اتفاقی می‌افتد؟

اسکریپت خودکار:
1. ✅ Python 3.11 را نصب می‌کند
2. ✅ PostgreSQL را راه‌اندازی می‌کند
3. ✅ Redis و Nginx را نصب می‌کند
4. ✅ پروژه را از GitHub دانلود می‌کند
5. ✅ Backend و Frontend را build می‌کند
6. ✅ همه چیز را تنظیم و اجرا می‌کند
7. ✅ اطلاعات ورود را نشان می‌دهد

**زمان نصب:** 5-10 دقیقه ⏱️

---

## 🌐 بعد از نصب

پنل در این آدرس‌ها در دسترس است:

```
http://YOUR_SERVER_IP:3000  → وب پنل
http://YOUR_SERVER_IP:8000  → API
```

اطلاعات ورود در انتهای نصب نمایش داده می‌شود!

---

## 💡 دستورات مفید

```bash
# مشاهده لاگ‌ها
sudo journalctl -u vpnmaster-backend -f

# راه‌اندازی مجدد
sudo systemctl restart vpnmaster-backend

# بررسی وضعیت
sudo systemctl status vpnmaster-backend
```

---

## 🐛 مشکل داری؟

مشاهده لاگ نصب:
```bash
cat /var/log/vpnmaster-install.log
```

یا [Issue](https://github.com/EchoDev7/vpn-master-panel/issues) بساز

---

**ساخته شده با ❤️ برای اینترنت آزاد**
