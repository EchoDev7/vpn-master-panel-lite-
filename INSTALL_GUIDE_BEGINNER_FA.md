# 🎓 راهنمای کامل نصب - برای افراد مبتدی

این راهنما برای کسی طراحی شده که **هیچ چیز نمی‌داند** و می‌خواهد VPN Master Panel را روی سرور Ubuntu 22.04 نصب کند.

---

## 📋 قبل از شروع

### چی نیاز دارید؟

1. ✅ **یک سرور** با Ubuntu 22.04
   - می‌تونه باشه: VPS، Cloud Server، یا کامپیوتر شخصی
   - حداقل 2GB RAM
   - 20GB فضای خالی

2. ✅ **دسترسی به سرور**
   - IP سرور
   - نام کاربری (معمولاً `root`)
   - رمز عبور

---

## 🔐 مرحله 1: اتصال به سرور

### در Windows:

**استفاده از PuTTY:**

1. **دانلود PuTTY:**
   - برو: https://www.putty.org/
   - دانلود و نصب

2. **اتصال:**
   ```
   Host Name: IP سرور شما (مثلاً: 192.168.1.100)
   Port: 22
   Connection Type: SSH
   
   کلیک: Open
   ```

3. **ورود:**
   ```
   login as: root
   Password: [رمز عبور سرور]
   ```

### در macOS/Linux:

باز کردن Terminal و تایپ:
```bash
ssh root@IP_SERVER

# مثال:
ssh root@192.168.1.100

# وارد کردن رمز عبور
```

**✅ اگر وارد شدید، موفق بودید!**

---

## 📥 مرحله 2: دانلود فایل نصب

### روش 1: دانلود مستقیم (آسان‌تر)

در Terminal سرور تایپ کنید:

```bash
# دانلود فایل نصب
wget https://raw.githubusercontent.com/EchoDev7/vpn-master-panel/main/install-simple.sh

# اجازه اجرا
chmod +x install-simple.sh

# اجرا
sudo bash install-simple.sh
```

### روش 2: از GitHub (اگر روش 1 کار نکرد)

```bash
# نصب Git
apt update
apt install -y git

# دانلود پروژه
git clone https://github.com/EchoDev7/vpn-master-panel.git
cd vpn-master-panel

# اجرای نصب
bash install-simple.sh
```

---

## ⚙️ مرحله 3: پاسخ به سوالات نصب

اسکریپت چند سوال می‌پرسه:

```
نام کاربری ادمین [admin]: 
→ Enter بزنید (یا اسم دلخواه تایپ کنید)

رمز عبور ادمین: 
→ یک رمز قوی تایپ کنید (حداقل 8 کاراکتر)

پورت وب پنل [8080]: 
→ Enter بزنید (یا پورت دلخواه، مثلاً 3000)

ایمیل ادمین [admin@local]: 
→ Enter بزنید (یا ایمیل خودتون)
```

**نکته:** هنگام تایپ رمز عبور، چیزی نمایش داده نمی‌شه! عادی است. تایپ کنید و Enter.

---

## ⏳ مرحله 4: صبر کنید

نصب شروع می‌شه و این مراحل انجام می‌شه:

```
[1/8] بروزرسانی سیستم...        ✓
[2/8] نصب Python...              ✓
[3/8] نصب دیتابیس...            ✓
[4/8] نصب ابزارهای VPN...       ✓
[5/8] ایجاد پوشه پروژه...       ✓
[6/8] دانلود فایل‌ها...         ✓
[7/8] نصب کتابخانه‌ها...        ✓
[8/8] راه‌اندازی سرویس...       ✓
```

**زمان تقریبی:** 5-10 دقیقه

---

## 🎉 مرحله 5: دسترسی به پنل

بعد از نصب، این اطلاعات رو می‌بینید:

```
╔══════════════════════════════════════════════╗
║     نصب با موفقیت انجام شد! 🎉              ║
╚══════════════════════════════════════════════╝

📍 آدرس پنل:
   http://192.168.1.100:8080

📍 مستندات API:
   http://192.168.1.100:8080/docs

📍 اطلاعات ورود:
   نام کاربری: admin
   رمز عبور: ********
```

### دسترسی:

1. **باز کردن مرورگر** (Chrome, Firefox, Safari, ...)
2. **تایپ آدرس:** http://IP_SERVER:8080
3. **ورود** با نام کاربری و رمز عبور

**✅ پنل شما آماده است!**

---

## 🔧 مرحله 6: دستورات مفید

### بررسی وضعیت پنل:

```bash
systemctl status vpnmaster
```

خروجی باید باشه:
```
● vpnmaster.service - VPN Master Panel
   Active: active (running)
```

### مشاهده لاگ‌ها (اگر مشکلی بود):

```bash
journalctl -u vpnmaster -f
```

برای خروج: `Ctrl + C`

### راه‌اندازی مجدد:

```bash
systemctl restart vpnmaster
```

### توقف پنل:

```bash
systemctl stop vpnmaster
```

### شروع دوباره:

```bash
systemctl start vpnmaster
```

---

## 🐛 مشکلات متداول و راه‌حل

### ❌ مشکل: "Connection refused"

**علت:** Firewall پورت را بسته است

**راه‌حل:**
```bash
# باز کردن پورت 8080
ufw allow 8080/tcp

# یا غیرفعال کردن موقت firewall
ufw disable
```

### ❌ مشکل: "systemctl: command not found"

**علت:** سیستم‌عامل Ubuntu نیست

**راه‌حل:** فقط روی Ubuntu 22.04 کار می‌کند

### ❌ مشکل: پنل باز نمی‌شود

**چک کردن:**
```bash
# 1. وضعیت سرویس
systemctl status vpnmaster

# 2. چک کردن پورت
netstat -tulpn | grep 8080

# 3. تست local
curl http://localhost:8080
```

### ❌ مشکل: "Permission denied"

**راه‌حل:** با `sudo` اجرا کنید:
```bash
sudo bash install-simple.sh
```

---

## 📚 مرحله 7: استفاده از پنل

### ساخت اولین کاربر VPN:

1. **ورود به پنل:** http://IP:8080
2. **کلیک Users** (کاربران)
3. **Add New User** (افزودن کاربر)
4. **پر کردن فرم:**
   ```
   Username: user1
   Password: ********
   Data Limit: 50 GB
   Expiry: 30 روز
   ```
5. **Save** (ذخیره)

### مشاهده آمار:

- **Dashboard:** آمار کلی
- **Traffic:** ترافیک مصرفی
- **Connections:** اتصالات فعال

---

## 🔐 نکات امنیتی (مهم!)

### 1. تغییر رمز پیش‌فرض:

```bash
# وارد سرور شوید
ssh root@IP_SERVER

# تغییر رمز
passwd
```

### 2. بستن پورت‌های غیرضروری:

```bash
# فقط پورت‌های ضروری
ufw allow 22/tcp      # SSH
ufw allow 8080/tcp    # Panel
ufw enable
```

### 3. بروزرسانی منظم:

```bash
apt update
apt upgrade -y
systemctl restart vpnmaster
```

---

## 📞 کمک بیشتر

### اگر مشکل داشتید:

1. **GitHub Issues:**
   https://github.com/EchoDev7/vpn-master-panel/issues

2. **لاگ‌ها رو بفرستید:**
   ```bash
   journalctl -u vpnmaster -n 50 > log.txt
   ```

3. **اطلاعات سیستم:**
   ```bash
   uname -a
   lsb_release -a
   ```

---

## 🎯 خلاصه دستورات

```bash
# نصب (یک بار)
wget https://raw.githubusercontent.com/.../install-simple.sh
chmod +x install-simple.sh
sudo bash install-simple.sh

# استفاده روزانه
systemctl status vpnmaster    # وضعیت
systemctl restart vpnmaster   # راه‌اندازی مجدد
journalctl -u vpnmaster -f    # لاگ‌ها

# دسترسی
http://IP_SERVER:8080
```

---

## ✅ چک‌لیست نصب موفق

- [ ] سرور Ubuntu 22.04 آماده است
- [ ] اتصال SSH موفق
- [ ] اسکریپت نصب دانلود شد
- [ ] نصب بدون خطا انجام شد
- [ ] پنل در مرورگر باز می‌شود
- [ ] ورود با admin موفق است
- [ ] اولین کاربر VPN ساخته شد

---

**تبریک! شما الان صاحب یک پنل مدیریت VPN هستید! 🎉**

این پنل:
- ✅ ساده و آسان
- ✅ بدون Docker
- ✅ مستقیم روی سرور
- ✅ آماده استفاده

**موفق باشید! 🚀**
